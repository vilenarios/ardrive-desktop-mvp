# UAT-RUN — autonomous safe-layer pass, 2026-07-04 [UAT]

**Runner:** TESTER agent (Claude Opus 4.8), autonomous.
**Base:** branch `uat/run-1` off `main @ d951a1e` (DESIGN-8 sweep merged).
**Build:** `npm run build` OK (dist/main/main.js 150 KB, dist/renderer/index.html present).
**Harness smoke:** `run-poc.js services` 18/18 PASS · `run-poc.js ui` 6/6 PASS — **the live Playwright-Electron
harness works in this worktree** (WSLg `DISPLAY=:0`, software render).
**Test suite:** `npm run test -- --run` → **58 files, 483 passed | 1 skipped**, 0 failed.
**New harness scripts added:** `scripts/uat/ui-onboarding.js`, `scripts/uat/ui-import-a11y.js` (reuse the POC pattern).
**Screenshots:** `scratchpad/uat-run-1/*.png` (7 captured — referenced by filename below; not committed).

> **Money rail honored:** zero funds moved, zero on-chain writes, no Stripe/checkout, no >100 KiB upload, no
> paid rename/manifest/conversion. All live-UI ran in disposable temp `userData`; no real profile touched.
> No wallet JSON / seed / password printed or committed. New scripts secret-scanned clean.

## Scope executed

- **SVC / RTL / STATIC** (deterministic, offline, $0): fully in scope — executed.
- **UI (live)**: only offline-reachable surfaces — first-run, Create-account wizard (all 3 steps), Import
  (wallet-file + recovery-phrase), drive-setup / welcome-back empty states, theme response, a11y ground-truth.
- **Live Dashboard / drive-list surfaces are genuinely unreachable offline** (see COVERAGE) — `case 'dashboard'`
  requires a truthy `drive`, which requires a real drive returned by the network `drive.list`. Their **component
  truth is covered by the passing RTL suites**; live render is deferred to a funded/supervised pass.

---

## 1. Results table (scenario → verdict · harness · evidence)

Verdict key: **PASS** executed & met expectation · **PASS(suite)** validated by a passing dedicated vitest suite +
code read · **DEFECT** scenario existed to catch a defect and it is confirmed present · **CLEARED** expected defect
no longer present (fixed by DESIGN-8/TRUST/POLISH) · **PARTIAL** part executed, rest deferred · **BLOCKED-Phil**
needs funds/human/real-wallet/on-chain · **NOT-RUN** not reached this pass.

### 1. Onboarding / wallet (ONB)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| ONB-1 | PASS | UI+STATIC | first-run heading/subtitle/`role=button`, brand accent bar + Permahills bg render — `onb1-firstrun-dark.png` |
| ONB-2 | PASS | UI+RTL | disabled logic runtime-verified (empty/`<8`/mismatch→disabled, valid→enabled) `WalletSetup.tsx:447`; "Important Security Notice" present (reworded, improved) — `onb2-password-valid.png` |
| ONB-3 | PASS | UI | seed step heading + "written down" checkbox gate runtime-verified; critical warning present — `onb3-seed-locked.png`/`onb3-seed-confirmed.png` |
| ONB-4 | PASS + DEFECT | UI+STATIC | dropzone works; **keyboard-inoperable** `<div role=null tabindex=null>` confirmed at runtime → A11Y-5 — `onb4-import-account.png` |
| ONB-5 | CLEARED | UI+SVC | runtime shows "3 words entered (12 or 24 expected)" + htmlFor-linked textarea; **H-COPY-1 fixed** — `onb5-recovery-phrase.png` |
| ONB-6 | PARTIAL | UI+SVC | local Create → welcome-back/drive-setup routing verified; import-from-file happy path (throwaway wallet) not driven |
| ONB-7 | NOT-RUN | SVC | ethereum/seed import handler envelopes — no dedicated harness written this pass |
| ONB-8 | PASS(suite) | SVC | `dev-env.test.ts` passed — autofill gated on `!isPackaged` |
| ONB-9 | CLEARED | STATIC | link now `href="https://docs.ardrive.io" target="_blank"` (`WalletSetup.tsx:383`), **not** `href="#"` |

### 2. Profiles (PROF)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| PROF-1 | PASS(suite) | RTL+SVC | `profile-authentication.test.ts` passed; eye-toggle aria-label gap noted (A11Y-4) |
| PROF-2 | PASS(suite) | RTL | `ux7-profile-management-login-error.test.tsx` passed |
| PROF-3 | NOT-RUN | SVC | delete two-step confirm — not driven this pass |
| PROF-4 | PASS | SVC | profile isolation proven by `poc-services` (2nd profile 0 mappings) |
| PROF-5 | PARTIAL | UI+RTL | switch-modal a11y hotspots STATIC-confirmed; live switch needs ≥2 profiles |
| PROF-6 | NOT-RUN | UI | add-new-profile from selector |
| PROF-7 | BLOCKED-Phil | SVC | OS keychain method/prompt — human/OS-gated |

### 3. Public drives (PUB)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| PUB-1 | PASS + DEFECT | RTL+UI+SVC | `create-drive-modal.test.tsx` passed; `DriveAndSyncSetup` `/32` counter + permanence warning STATIC-verified; **H-COPY-2** 32-UI vs 100-validator confirmed |
| PUB-2 | BLOCKED-Phil | UI+CHAIN | on-chain drive create (network write) |
| PUB-3 | BLOCKED-Phil | CHAIN | create-public-drive net-zero (network write) |
| PUB-4 | PASS + DEFECT | RTL+STATIC | `trust5-setup-success-icon.test.tsx` passed; **H-GW-1** "View" opens `arweave.net` `SetupSuccessScreen.tsx:50` |
| PUB-5 | PARTIAL | UI+RTL+SVC | `addDriveMapping` proven by `poc-services`; live add needs unmapped drive |
| PUB-6 | PARTIAL / BLOCKED-Phil | UI+CHAIN | **H-UX-2** native `window.confirm` for switch confirmed (`Dashboard.tsx:384,469`); paid rename branch blocked |

### 4. Private drives + unlock (PRIV)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| PRIV-1 | PASS(suite) | RTL+UI | permanence-warning copy + `>=8`/confirm rules STATIC-verified (`CreateDriveModal.tsx:58`) |
| PRIV-2 | BLOCKED-Phil | CHAIN | create-private-drive on-chain |
| PRIV-3 | PASS(suite) | UI+RTL+CHAIN | `drive-selector-unlock.test.tsx` passed |
| PRIV-4 | PASS(suite) | RTL+SVC | `ux3-unlock-error-display.test.tsx` + `drive-unlock-validation.test.ts` passed — wrong-pw vs net-error differentiated |
| PRIV-5 | PASS(suite) | SVC+UI | `drive-key-persistence.test.ts` passed — opt-in persist + forget |
| PRIV-6 | PASS(suite) | SVC | `drive-key-manager.test.ts` passed — key cleared on switch/lock |
| PRIV-7 | BLOCKED-Phil | CHAIN | private round-trip on-chain |

### 5. Sync upload (UPL)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| UPL-1 | PASS(suite) | UI+SVC | `sync-manager.test.ts` passed (detection→pending); live drop needs active drive |
| UPL-2 | PASS | SVC | `poc-services` + `CostCalculator.test.ts` — 40 KiB free / 200 KiB not |
| UPL-3 | PASS(suite) | SVC+STATIC | `CostCalculator.isFileTooBig`; `poc-services` 100 MiB check |
| UPL-4 | PASS(suite) | SVC+UI | `FileStateManager.test.ts` passed — move/rename classification |
| UPL-5 | BLOCKED-Phil | CHAIN | edit→revision on-chain |
| UPL-6 | PASS(suite) | SVC | `database-manager.test.ts` — crash recovery flips to `failed` w/ honest "may or may not have reached Arweave" copy |

### 6. Sync download (DL)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| DL-1 | PASS | UI+RTL | empty-state copy STATIC-verified (`DownloadQueueTab.tsx:190-200`) |
| DL-2 | PASS(suite) | UI+SVC | `download-manager.test.ts` passed |
| DL-3 | PASS(suite) | SVC+CHAIN | `download-manager-private-realfs.test.ts` passed — decrypt byte-exact |
| DL-4 | PARTIAL | UI+RTL | controls STATIC; live needs queued download |
| DL-5 | PASS(suite) | SVC | `streaming-downloader-flapping.test.ts` passed |
| DL-6 | NOT-RUN | SVC+UI | re-download-all |

### 7. Upload approval / cost (APPR)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| APPR-1 | PASS(suite) | UI+RTL | `UploadApprovalQueueModern.test.tsx` passed — banner/columns |
| APPR-2 | PASS(suite) | UI+RTL | FREE badge <100 KiB |
| APPR-3 | BLOCKED-Phil | UI+CHAIN | approve free-tier = network write; logic covered by `main-approve-handlers.test.ts` |
| APPR-4 | PASS | RTL+SVC | **money backstop directly verified** — `main.ts:2185` throws "Insufficient Turbo Credits" when balance<required; `money1-turbo-only.test.tsx` passed |
| APPR-5 | PASS | SVC+RTL | `poc-services` MONEY-3 + `money3-dbshape-repro.test.tsx` passed |
| APPR-6 | PASS(suite) | RTL+SVC | `upload-retry-guard.test.ts` passed (MONEY-2 retry admission) |
| APPR-7 | PASS(suite) | RTL+UI | `upload-queue-no-conflict-ui.test.tsx` passed |

### 8. Turbo credits / payment (TURBO)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| TURBO-1 | PASS(suite) | UI+RTL | `TurboCreditsManager.test.tsx` passed; **tabs lack `role=tab`** noted (A11Y-6) |
| TURBO-2 | PARTIAL | SVC+RTL | fiat estimate covered by suite; live read deferred |
| TURBO-3 | PASS(suite) / BLOCKED-Phil | RTL+UI | `payment-window.test.ts` passed (window-open guard); real charge needs Phil |
| TURBO-4 | PASS / BLOCKED-Phil | RTL | fee copy directly verified (`TurboPurchaseTab.tsx:126-127` "~23% … non-transferrable"; NaN→"Unavailable" :118); real conversion needs Phil |
| TURBO-5 | PASS(suite) | RTL | `money6-topup-refresh.test.tsx` passed |
| TURBO-6 | PASS(suite) | RTL+UI | usage-stats tiles (TRUST-1) covered |
| TURBO-7 | PASS(suite) | RTL | `UserMenu-nan-balance.test.tsx` + `wallet-manager-secure-nan-balance.test.ts` passed |

### 9. Settings / gateway (SET)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| SET-1 | PASS(suite) | UI+RTL | `settings-folder.test.tsx` passed; **modal not `role=dialog`** noted (A11Y-2) |
| SET-2 | PASS(suite) | SVC+UI | `drive-mapping-folder-persistence.test.ts` + `sync-folder-change.test.ts` passed |
| SET-3 | PASS(suite) | RTL+SVC | `settings-gateway.test.tsx` + `gateway.test.ts` passed |
| SET-4 | PASS(suite) | RTL+SVC | reset-to-default (`gateway.test.ts`) |
| SET-5 | PARTIAL | UI+SVC | theme system responds (light body-bg `rgb(247,247,247)` verified; persistence via `config.setTheme` in `ThemeContext.tsx:93`); full toggle UI needs dashboard |
| SET-6 | PASS(suite) / HUMAN | RTL+UI | `WalletExport.test.tsx` passed; real key verification human-only |

### 10. Activity (ACT)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| ACT-1 | PASS | UI+RTL | "No Drive Selected"/"No recent activity"/"Upload or download files…" STATIC-verified (`ActivityTab.tsx:573,848,853`) |
| ACT-2 | BLOCKED-Phil | UI+RTL | needs a real upload to show "Permanent" chip |
| ACT-3 | PARTIAL | RTL+UI | search/filter needs data |
| ACT-4 | PASS(suite) | RTL+UI | `activity-tab-copy-link.test.tsx` + `activity-tab-context-menu-a11y.test.tsx` passed — this menu is a11y-correct |
| ACT-5 | PASS + DEFECT | RTL+UI+STATIC | **H-UX-1** Retry does `window.location.reload()` `ActivityTab.tsx:1209,1225` |

### 11. Storage / permaweb (PERM)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| PERM-1 | PASS | RTL+UI | 3-way state copy STATIC-verified (fetch-fail vs new-drive vs empty, `StorageTab.tsx:1153-1165`) |
| PERM-2 | PARTIAL | UI+RTL | list/grid/breadcrumb needs drive data |
| PERM-3 | PARTIAL | RTL+STATIC | STATUS_META present; **status = color+icon+title only, no live-region** (A11Y) |
| PERM-4 | NOT-RUN | UI+SVC | free-up-space (reversible) — needs drive data |
| PERM-5 | BLOCKED-Phil | UI+CHAIN | permaweb-after-upload |
| PERM-6 | NOT-RUN | RTL+UI | export-CSV; native `alert()` on empty noted |

### 12. Hide / restore (HIDE)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| HIDE-1 | PASS(suite) | SVC+UI | `file-operation-detector-hide.test.ts` passed |
| HIDE-2 | BLOCKED-Phil | CHAIN | hide writes metadata on-chain |
| HIDE-3 | NOT-RUN | RTL+UI | hidden badge — needs data |
| HIDE-4 | BLOCKED-Phil | UI+CHAIN | unhide restores (reversible, but on-chain) |

### 13. Manifests (MAN)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| MAN-1 | PARTIAL | RTL+UI+STATIC | modal copy present; **folder tree rows are click `<div>`s, no `role=tree`** (A11Y-5) |
| MAN-2 | NOT-RUN | RTL+UI | confirm-FREE dialog — needs folder data |
| MAN-3 | BLOCKED-Phil | CHAIN | create manifest on-chain |
| MAN-4 | BLOCKED-Phil | CHAIN | replace manifest on-chain |

### 14. Error / edge states (ERR)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| ERR-1 | PASS(suite) | RTL+UI | `ux7-boot-error-routing.test.tsx` passed — offline existing-profile → boot-error, never wallet-setup |
| ERR-2 | PASS + minor | RTL+STATIC | `ErrorBoundary.tsx` "Something went wrong"/"Try Again"/"Reload App"/`reportError` present; **no `role="alert"`** (a11y) |
| ERR-3 | DEFECT | SVC | **H-BND-1** 102400 B: `CostCalculator.ts:88` `<` (not free) vs `main.ts:2174,2362` `<=` (free) — confirmed by `poc-services` |
| ERR-4 | PARTIAL | SVC+STATIC | 96 `ipcMain.handle`; several confirmed envelope (`profiles.list`, `config.get`); full raw-vs-envelope audit deferred (known trap, CLAUDE.md) |
| ERR-5 | PASS(suite) | SVC | `database-migrations.test.ts` + `migration-adversarial.test.ts` passed; `poc-services` v3→v5 |
| ERR-6 | PASS | SVC | insufficient-funds backstop = APPR-4 throw path verified |

### 15. Theme light/dark (THEME)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| THEME-1 | PARTIAL / HUMAN | UI+STATIC | default preference is **`system`** (`ThemeContext.tsx:48`), which followed OS→light on this box; dark is the fallback (`:35`). Onboarding screen is theme-invariant brand art → full dark-surface visual needs dashboard + human |
| THEME-2 | PARTIAL / HUMAN | UI+STATIC | light body-bg `rgb(247,247,247)` verified (token system responds) — `theme2-firstrun-light.png`; full themed-surface visual needs dashboard + human |
| THEME-3 | DEFECT | STATIC | **H-TOKEN-1** raw hex outside `styles/theme.css`: `styles.css:334` (dead `.app` rule), `styles.css:2065/2069/2077/2081` (live file-type-icon colors), dead-code `StoredFilesBrowser.tsx:283 #4A90E2`, `MetadataEditor.tsx` `white`+`--blue-*` |

### 16. A11y / keyboard (A11Y)
| ID | Verdict | Harness | Evidence |
|---|---|---|---|
| A11Y-1 | DEFECT | RTL+STATIC | **H-A11Y-1** no `aria-live`/`role` on `ToastContainer.tsx:11` or `ToastNotification.tsx:66`; close btn no aria-label |
| A11Y-2 | DEFECT | STATIC+RTL | **H-A11Y-2** grep: **0** components use `role="dialog"`/`aria-modal` |
| A11Y-3 | PARTIAL | RTL+STATIC | mixed: recovery textarea + gateway input htmlFor-linked (good); **TurboPurchase currency `<select>` unlabeled** (`TurboPurchaseTab.tsx:72`); PasswordInput uses wrapping `<label>` (implicit) + eye aria-labeled |
| A11Y-4 | DEFECT | STATIC+RTL | StorageTab **22 `title=`, 0 `aria-label`**; icon-only buttons title-only across StorageTab/DownloadQueue/Modern queue |
| A11Y-5 | DEFECT | STATIC+RTL+UI | **H-A11Y-3** dropzone (runtime `div role=null tabindex=null`) + `ActivityTab.tsx:626` rows are click `<div>`s, no role/tabIndex/onKeyDown |
| A11Y-6 | DEFECT | STATIC+RTL | **H-A11Y-4** `TabNavigation.tsx:36` emits `aria-controls="{id}-panel"` but Dashboard has **no matching id / no `role=tabpanel`**; Turbo tabs lack `role=tab` |
| A11Y-7 | DEFECT | STATIC+RTL | grep: **0** `role="progressbar"`/`aria-valuenow` app-wide — progress bars are styled `<div>`s |

---

## 2. §17 Known-issue hotspot verdicts

| ID | Verdict | Note |
|---|---|---|
| **H-A11Y-1** toasts no aria-live | **CONFIRMED** | `ToastContainer.tsx:11`, `ToastNotification.tsx:66` — no `role`/`aria-live`; close btn unlabeled |
| **H-A11Y-2** no modal is `role=dialog` | **CONFIRMED** | 0 components match `role="dialog"`/`aria-modal` |
| **H-A11Y-3** clickable divs + dropzone | **CONFIRMED** | dropzone runtime `div role=null tabindex=null`; `ActivityTab.tsx:626` rows |
| **H-A11Y-4** tab `aria-controls` → missing panels | **CONFIRMED** | `TabNavigation.tsx:36` vs Dashboard (no `-panel` id, no `role=tabpanel`) |
| **H-COPY-1** seed "exactly 12 words" | **CLEARED** | now uses validator msg + "12 or 24 expected" (TRUST-3); runtime-verified |
| **H-COPY-2** drive-name 32 UI vs 100 validator | **CONFIRMED** | `input-validator.ts:38 =100` vs UI hardcoded 32 (`CreateDriveModal.tsx:39,233`, `DriveAndSyncSetup.tsx:395`) |
| **H-BND-1** free-tier 102400 `<` vs `<=` | **CONFIRMED** | `CostCalculator.ts:88` `<` vs `main.ts:2174,2362` `<=` |
| **H-GW-1** SetupSuccess opens arweave.net | **CONFIRMED** | `SetupSuccessScreen.tsx:50` `window.open('https://arweave.net/'+txId)` |
| **H-DEAD-1** dead code (raw hex) | **CONFIRMED** | `UploadApprovalQueue.tsx`, `MetadataEditor.tsx`, `StoredFilesBrowser.tsx` all unimported (0 refs) |
| **H-UX-1** Activity Retry full reload | **CONFIRMED** | `ActivityTab.tsx:1209,1225` `window.location.reload()` |
| **H-TOKEN-1** competing token families / raw hex | **CONFIRMED** | live `styles.css:2065-2081` file-type-icon hex; `MetadataEditor` `--blue-*`+`white` (dead) |
| **H-UX-2** native confirm/alert | **CONFIRMED** | `Dashboard.tsx:384,469` `window.confirm`; `:550,559` `alert()` ("TODO: Replace with proper toast") |

**12/12 hotspots adjudicated: 11 CONFIRMED, 1 CLEARED (H-COPY-1).** Bonus: ONB-9 dead link also CLEARED.

---

## 3. Ranked defect list

Severity: **S1** money/data-integrity · **S2** broken/blocking UX or a11y-blocker · **S3** consistency/polish ·
**S4** cleanup. None of these moved funds; all are latent.

| # | Sev | Class | Scenario | Defect | Location | Repro / note | Shot |
|---|---|---|---|---|---|---|---|
| 1 | S2 | FUNC | ERR-3 | Free-tier boundary inconsistent at exactly 102400 B — `CostCalculator` says NOT free, approval gate says FREE | `CostCalculator.ts:88` `<` vs `main.ts:2174,2362` `<=` | a 102400-byte file shows a cost in one path, FREE in the other | — |
| 2 | S2 | A11Y | A11Y-2 | No modal is a real dialog — no `role=dialog`/`aria-modal`/focus-trap on any of ~11 modals | all `*Modal.tsx`, Settings, ProfileSwitcher | screen-reader/keyboard users can't scope or escape modals | — |
| 3 | S2 | A11Y | A11Y-1 | Toasts silent to SRs — no `aria-live`/`role=alert|status`; payment/copy outcomes never announced | `ToastContainer.tsx:11`, `ToastNotification.tsx:66` | fire a toast, inspect — no live region | — |
| 4 | S2 | A11Y | A11Y-5 | Keyboard-inoperable click targets — wallet dropzone + Activity rows are `<div onClick>` with no role/tabIndex/onKeyDown | dropzone `WalletSetup.tsx:635`, `ActivityTab.tsx:626` | Tab lands nowhere on them; runtime `div role=null tabindex=null` | `onb4-import-account.png` |
| 5 | S2 | A11Y | A11Y-6 | Tab `aria-controls` points at non-existent panel IDs; panels lack `role=tabpanel` | `TabNavigation.tsx:36` vs `Dashboard.tsx` | SR "controls" jumps to nothing; Turbo tabs have no `role=tab` | — |
| 6 | S2 | A11Y | A11Y-7 | Progress bars have no `role=progressbar`/`aria-valuenow` (0 app-wide) | SyncProgressDisplay, StatusPill | SR can't read upload/download progress | — |
| 7 | S3 | UX | H-UX-1 / ACT-5 | "Retry Download" does a full `window.location.reload()` — jarring app remount | `ActivityTab.tsx:1209,1225` | click Retry in Activity details → whole app reloads | — |
| 8 | S3 | UX | H-UX-2 / PUB-6·APPR-6 | Native `window.confirm`/`alert()` for drive switch + approve-all errors (unstyleable, one even flags its own TODO) | `Dashboard.tsx:384,469,550,559` | switch drive → OS confirm dialog | — |
| 9 | S3 | CONSISTENCY | PUB-4 / H-GW-1 | Setup-success "View" opens `arweave.net`, violating the turbo-gateway.com read rail | `SetupSuccessScreen.tsx:50` | create drive → View → arweave.net tab | — |
| 10 | S3 | A11Y | A11Y-4 | Icon-only buttons rely on `title=` only (StorageTab 22×, DownloadQueue rows, Modern queue) | `StorageTab.tsx` (22 `title`, 0 `aria-label`) | SR announces nothing useful | — |
| 11 | S3 | CONSISTENCY | H-COPY-2 / PUB-1 | Drive-name cap 32 in UI vs 100 in `validateDriveName` | `input-validator.ts:38` vs `CreateDriveModal.tsx:39` | two different limits for one field | `pub1-drive-setup.png` |
| 12 | S3 | A11Y | A11Y-3 | TurboPurchase currency `<select>` unlabeled | `TurboPurchaseTab.tsx:72` | no `<label>`/aria-label | — |
| 13 | S3 | CONSISTENCY | THEME-3 / H-TOKEN-1 | Live raw hex outside token file (file-type-icon colors) + competing `--ardrive-*`/`--blue-*` families | `styles.css:2065,2069,2077,2081` | some icon colors hex, siblings tokenized | — |
| 14 | S4 | A11Y | ERR-2 | ErrorBoundary screen has no `role="alert"` | `ErrorBoundary.tsx:85` | full-screen error not announced | — |
| 15 | S4 | CLEANUP | H-DEAD-1 / THEME-3 | 3 unimported dead components still shipping (raw hex `#4A90E2`, `white`, `--blue-*`) | `UploadApprovalQueue.tsx`, `MetadataEditor.tsx`, `StoredFilesBrowser.tsx` | 0 import refs; delete to kill token violations | — |
| 16 | S4 | CLEANUP | THEME-3 | Dead `.app { background:#f9fafb }` rule (no `className="app"` anywhere; shell is `.app-container`) | `styles.css:334` | orphan rule, raw hex | — |

> No **S1** defect found in the executed layers. The two S1-critical money backstops (**APPR-4** insufficient-balance
> throw, **APPR-5** MONEY-3 DB normalization) were directly verified as **working**.

---

## 4. Coverage summary

**96 scenarios total — counts add to 96:**

- **Firm verdict — executed with evidence: 61**
  - PASS / PASS(suite) / PASS(guard): **48**
  - **DEFECT-confirmed: 11** — ONB-4, PUB-4, ACT-5, ERR-3, THEME-3, A11Y-1, A11Y-2, A11Y-4, A11Y-5, A11Y-6, A11Y-7
  - **CLEARED: 2** — ONB-5 (H-COPY-1), ONB-9 (dead link)
- **PARTIAL (core executed, remainder deferred): 14** — ONB-6, PROF-5, PUB-5, PUB-6, DL-4, TURBO-2, SET-5, ACT-3, PERM-2, PERM-3, MAN-1, ERR-4, THEME-1, THEME-2
- **BLOCKED-needs-Phil (funds / on-chain / human / real-wallet): 13** — PROF-7, PUB-2, PUB-3, PRIV-2, PRIV-7, UPL-5, APPR-3, ACT-2, PERM-5, HIDE-2, HIDE-4, MAN-3, MAN-4. *(TURBO-3/TURBO-4 counted under PASS(guard): the window-open / fee-copy / NaN guard was tested; only the real charge is blocked.)*
- **NOT-RUN this pass: 8** — ONB-7, PROF-3, PROF-6, DL-6, PERM-4, PERM-6, HIDE-3, MAN-2

Total: **61 + 14 + 13 + 8 = 96.** Net **75 scenarios touched with real evidence** (61 firm + 14 partial), 13
blocked on funds/human, 8 not-run. The 58 vitest suites (483 tests) underpin most PASS(suite) verdicts;
`poc-services` and the two new UI scripts underpin the SVC/UI ones.

### Precisely deferred to a supervised / funded pass (with reason)

**On-chain / funded (INFRA-9 wallet + budget):** PUB-2, PUB-3, PRIV-2, PRIV-7, UPL-5, APPR-3, PERM-5, HIDE-2,
HIDE-4, MAN-3, MAN-4, ACT-2 — each requires a real ArFS write or a produced upload. All are net-zero free-tier
in principle but touch the network and were kept out of this offline pass per the money rail.

**Real payment (Phil's budget):** TURBO-3 (Stripe checkout completion), TURBO-4 (AR→Credits conversion) — only the
**guard** was tested; completing a charge needs Phil.

**Human/OS-gated:** PROF-7 (OS keychain prompt), SET-6 real key reveal, THEME-1/THEME-2 pixel/brand judgement.

**Live-Dashboard-dependent (need a real drive in `drive.list`):** the authenticated-surface halves of PROF-5,
PUB-5, PUB-6, DL-4, DL-6, TURBO-2, SET-5, ACT-3, PERM-2/3/4/6, HIDE-3, MAN-1/2 — component truth is covered by the
passing RTL suites; only the **live render / interaction** is deferred.

**Not attempted (time-box, no dedicated harness) — the 8 NOT-RUN:** ONB-7 (import-handler envelopes), PROF-3
(delete two-step confirm), PROF-6 (add-profile-from-selector), DL-6 (re-download-all), PERM-4 (free-up-space),
PERM-6 (export-CSV), HIDE-3 (hidden badge), MAN-2 (confirm-FREE dialog). ERR-4 is PARTIAL (several handlers spot-
checked as envelope; the full 96-handler audit is what remains).

### Honesty notes (things the plan expected that differ in reality)
- **THEME default is `system`, not hard "dark".** On a light-preferring OS the app renders light by design — not a
  bug, but THEME-1's literal "dark baseline on default launch" only holds on a dark-preferring OS.
- **The onboarding screen is theme-invariant** (fixed dark brand backdrop + white card) — light/dark visual diff
  can only be judged on the dashboard, which is offline-unreachable here.
- **Several plan line numbers are pre-DESIGN-8** (e.g. H-COPY-1 @725, dropzone @594, H-GW-1 @42) — I re-anchored
  each to the current line and confirmed the underlying condition, not the stale coordinate.
